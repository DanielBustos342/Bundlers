const suma = (a, b) => a + b;

const resta = (a, b) => a - b;

//exports es un objeto
module.exports = {
    //sintaxis de objetos literales
    suma,
    resta,
    // multiplicacion,
    // division,
    // potencia
};

console.log(exports);
